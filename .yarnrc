# Yarn configuration file
# This tells Yarn to ignore engine version mismatches
--ignore-engines true

# Ignore platform checks (useful for cross-platform development)
# --ignore-platform true

# Exact version locking (recommended for consistency)
--exact true
